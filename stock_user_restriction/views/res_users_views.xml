<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form" model="ir.ui.view">
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Stock Restrictions" attrs="{'invisible': [('login','=', 'admin')]}">
                    <group>
                        <field name="picking_type_ids"/>
                        <field name="location_restriction"/>
                        <field name="location_done_ids"
                            attrs="{'invisible': [('location_restriction','!=', True)]}"/>
                        <field name="location_cancel_ids"
                            attrs="{'invisible': [('location_restriction','!=', True)]}"/>
                    </group>
                </page>
            </notebook>
        </field>
    </record>
</odoo>
